node_modules/

# Optional org CA for npm TLS (never commit PEMs — use npm run deps)
.certs/*.pem
.certs/*.crt
!.certs/.gitkeep

# Local build outputs
/dist*/
/OLD/

# Installers belong in Releases / your public storefront repo — not in Git history (size + optics).
releases/*.exe
releases/*.dmg
releases/*.pkg

# Electron user data & logs should never be committed
*.log
*.log.*
settings.json
install_salt.txt

# OS/IDE
.DS_Store
Thumbs.db
.vscode/
